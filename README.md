A simple Python TCP chatroom using a client/server architecture with encrypted communication.  

Requirements
- Python 3.10+

Installation
Clone the repository and install dependencies:


git clone https://github.com/benny-e/chatroom   
cd chatroom  
python -m venv .venv  
source .venv/bin/activate  
pip install -e .  

